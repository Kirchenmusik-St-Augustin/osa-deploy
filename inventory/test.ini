# PLACEHOLDER -- no real "test" VPS exists yet. APP_ENVIRONMENT=test is
# primarily the automated-test (pytest/CI) value, not necessarily a
# deployed target -- kept here for completeness alongside the other three
# valid APP_ENVIRONMENT values. Fill in real values only if a dedicated
# test VPS is actually provisioned.
[vps]
CHANGEME.example.invalid

[vps:vars]
app_environment=test
backend_domain=CHANGEME.example.invalid
frontend_domain=CHANGEME.example.invalid
shorturl_domain=CHANGEME.example.invalid
