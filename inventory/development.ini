# PLACEHOLDER -- no real "development" VPS exists yet. Local development
# runs directly via Podman on the dev machine itself, not through this
# repo. Fill in real values here only once a dedicated development VPS is
# actually provisioned; until then this file is not meant to be used.
[vps]
CHANGEME.example.invalid

[vps:vars]
app_environment=development
backend_domain=CHANGEME.example.invalid
frontend_domain=CHANGEME.example.invalid
shorturl_domain=CHANGEME.example.invalid
